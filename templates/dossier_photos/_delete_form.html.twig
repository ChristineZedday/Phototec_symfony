<form method="post" action="{{ path('dossier_photos_delete', {'id': dossier_photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier_photo.id) }}">
    <button class="btn">Delete</button>
</form>
