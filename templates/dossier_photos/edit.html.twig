{% extends 'base.html.twig' %}

{% block title %}Edit DossierPhotos{% endblock %}

{% block body %}
    <h1>Edit DossierPhotos</h1>

    {{ include('dossier_photos/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('dossier_photos_index') }}">back to list</a>

    {{ include('dossier_photos/_delete_form.html.twig') }}
{% endblock %}
